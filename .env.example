# ─── GodLocal Environment Config ───────────────────────────────────────────

# Model settings
DEFAULT_MODEL=qwen2.5:7b         # or llama3.2:3b, mistral:7b, gemma2:9b
OLLAMA_BASE_URL=http://ollama:11434

# Memory
CHROMA_TOKEN=godlocal_secret     # change in production!
SLEEP_CYCLE_HOUR=1               # 01:00 nightly hippocampal-replay

# Soul
SOUL_FILE=godlocal_data/souls/default.md

# Optional: AirLLM (for GPU with low VRAM — 4-bit slice loading)
# AIRLLM_MODEL_PATH=/app/models/Qwen2.5-7B-Instruct
# USE_AIRLLM=true
